# Teardown

Before ending a session:

1. Ensure all modified files compile: `cargo build`.
2. Run the full test suite: `cargo test --workspace`.
3. Summarize all changes made (files modified, features added, bugs fixed).
4. Note any remaining work or known issues for the next session.
5. Do NOT push to git or create PRs without explicit user confirmation.

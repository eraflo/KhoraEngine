documents:
  - path: README.md
    tags: [index, navigation, identity, workflow]
    priority: high
    always_load: true
  - path: rules.md
    tags: [rules, constraints, must-always, must-never]
    priority: high
    always_load: true
  - path: conventions.md
    tags: [style, conventions, naming, patterns, components]
    priority: high
    always_load: true
  - path: architecture.md
    tags: [architecture, clad, crates, dependencies, traits, components, file-locations]
    priority: high
    always_load: true
